node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.vs/
.env
.env.*
!.env.example
dev-dist

# Capacitor native projects — regenerated fresh in CI (npx cap add android)
android/
ios/
*.keystore
*.jks
# Signing-key material — lives in CI secrets (ANDROID_KEYSTORE_B64), never the repo
*.keystore.b64
release.keystore.b64
